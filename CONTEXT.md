# WasabiPad コンテキスト

WasabiPad の利用者が変更できる設定、文書作業を再開するための状態、ファイルツリー上の操作対象を区別するための用語集。

## 設定

**アプリ設定**:
WasabiPad 全体の見た目・編集・プレビュー・起動・検索など、利用者が継続的に選ぶ設定。変更は即時反映し、自動保存する。
_Avoid_: 作業状態、セッション状態

**クイック設定**:
アドレスバー右のギアから開く、頻繁に変更するアプリ設定の入口。
_Avoid_: 設定ページ、ツールバー設定

**設定モーダル**:
クイック設定から開く、アプリ設定をカテゴリ別にまとめた正規の管理画面。
_Avoid_: 設定ウィンドウ、別設定画面

**既存ショートカット**:
ステータスバーや検索画面など、設定モーダル以外に残る設定変更の入口。設定モーダルと同じ設定を更新する。
_Avoid_: 別設定、旧設定

## 状態

**セッション状態**:
再起動時に作業を復元するための再開タブとアクティブタブ。アプリ設定の初期化では消去しない。
_Avoid_: アプリ設定、ユーザー設定

**タブ別ファイルツリー表示状態**:
タブごとに保持する、ファイルツリーの通常表示におけるフォルダの展開・折りたたみの状態。同じタブへ戻ったときに復元するが、アプリ再起動時やタブを閉じた後の復元対象にはしない。
_Avoid_: ファイルツリー全体状態、セッション状態

**フォルダタブのルート**:
フォルダを表示しているタブが、表示の起点として保持するフォルダ。ドライブやファイルパスの最上位と必ずしも同じではない。
_Avoid_: ファイルシステムのルート、現在のファイルの親フォルダ

**タブ別ファイルツリー検索状態**:
タブごとに保持する、ファイルツリー検索の検索条件、検索結果一覧、選択箇所、結果グループの開閉表示。同じタブへ戻ったときに復元するが、検索実行中の一時状態、アプリ再起動、タブを閉じた後は復元対象にしない。
_Avoid_: 共有検索状態、セッション状態

## Markdown表示

**Markdownリンク**:
Markdown文書から、別の文書、同じ文書内の位置、または外部Webページを参照するリンク。
_Avoid_: リンク、ハイパーリンク

**文書内フラグメント**:
Markdown文書内の見出しまたは明示アンカーへ移動するためのリンク先指定。
_Avoid_: ページ内リンク、fragment

**外部Webリンク**:
HTTPまたはHTTPSで外部Webページを参照するMarkdownリンク。
_Avoid_: 外部URL

**起点タブ**:
Markdownリンクの操作を開始した文書を表示しているタブ。リンク先のタブ操作はこのタブを基準にする。
_Avoid_: 現在タブ、元タブ

## ファイルツリー

**ファイルツリー**:
WasabiPad のサイドバーに表示する、ファイルとフォルダを階層で確認・操作するための表示。実ファイル項目と仮想項目を含む。
_Avoid_: フォルダツリー

**表示中ファイル**:
ファイルツリーで現在の展開状態により行として表示できるファイル。アーカイブ内の仮想ファイル項目を含む。画面外でもスクロールで到達できる行を含み、折りたたまれたフォルダ配下のファイルは含まない。
_Avoid_: 全ファイル、読み込み済みファイル

**実ファイル項目**:
OS のファイルシステム上に実在するファイルまたはフォルダ。ファイルツリー上のコピー、切り取り、貼り付け、名前変更、削除などの操作対象。
_Avoid_: 実体、リアルファイル

**仮想項目**:
アーカイブなどの内部に表示される、OS のファイルシステム上で独立したパスを持たない項目。ファイルツリー上では閲覧対象だが、実ファイル項目として操作しない。
_Avoid_: アーカイブファイル、仮想ファイル

**内部クリップボード**:
WasabiPad 内で実ファイル項目のコピーまたは切り取り状態を一時保持する仕組み。OS のクリップボードとは区別する。
_Avoid_: ファイルクリップボード、共有クリップボード

**外部変更**:
WasabiPad の外部で行われたファイルまたはフォルダの作成、変更、移動、削除。ファイルツリーは外部変更を自動更新で反映する。
_Avoid_: 外部更新、手動更新

**文書画像フォルダ**:
Markdown文書に貼り付けた画像を、その文書と対応付けて保持するフォルダ。文書の移動や名前変更に追従する。
_Avoid_: 画像フォルダ、image_markdown

## 画面表示

**OSウィンドウ状態**:
OSが管理するウィンドウの最小化・最大化・復元状態。アプリ内のペイン表示状態とは独立し、復元時の大きさと位置はOSの復元範囲を基準にする。
_Avoid_: 画面状態、ペイン状態

**アプリ内ペイン表示状態**:
メイン画面のファイルツリーとプレビューに対する開閉・全画面表示の状態。最小化中も保持し、表示領域が狭いときは本文を守るため一時的に補助ペインを退避する。
_Avoid_: OSウィンドウ状態、表示フラグ

**有効ビューポート**:
画面表示の確定に使える、正の幅と高さを持つ最新の表示領域。最小化中などの無効な寸法では直前の有効ビューポートを置き換えない。
_Avoid_: ウィンドウサイズ、測定値

**安定表示**:
ウィンドウ状態・ペイン状態・最新の内容が表示へ反映された後の画面。古い内容、空白、白く抜けた未描画領域を残さない状態を指す。
_Avoid_: 描画完了、再描画済み
