{
  "name": "petapad-ui",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:ipc": "node scripts/check-ipc-contract.mjs",
    "check:config": "node scripts/check-config.mjs",
    "sync:version": "node scripts/sync-version.mjs",
    "test": "vitest run",
    "build": "npm test && npm run check:config && npm run check:ipc && tsc --noEmit && vite build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5",
    "vite": "^6",
    "vitest": "^4.1.10"
  }
}
