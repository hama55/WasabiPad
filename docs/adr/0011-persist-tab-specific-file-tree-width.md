# タブ別ファイルツリー幅を再起動後も復元する

ファイルツリーの手動幅は、既存の `openTabs` セッション状態にある各 `StoredTab.fileTreeWidth` へ保存し、タブID単位でアプリ再起動後も復元する。幅がない旧形式・新規タブ・不正値は共通の `sidebarWidth` を使い、閉じたタブを再開した場合は幅を引き継がない。通常ファイル・空タブでは共通の既定幅を使い、ファイルツリーを持つフォルダ・アーカイブ表示だけ保存幅を適用することで、新しい保存キーやパス単位の幅管理を追加せず、既存のタブ保存経路と互換性を保つ。
