# アプリ設定をギア起点へ集約する

アプリ設定はアドレスバー右のギアからクイック設定と設定モーダルで管理し、変更は即時反映・自動保存する。既存のステータスバーや検索画面の操作はショートカットとして残し、同じ設定状態を更新する。初期化の対象はアプリ設定に限定し、再開タブとアクティブタブからなるセッション状態は保持する。設定を一箇所へ集約しつつ既存の操作習慣と作業復元を壊さないため、この構成を採用する。
