# アプリ設定をギア起点へ集約する

アプリ設定はアドレスバー右のギアから設定モーダルで管理し、変更は即時反映・自動保存する。検索や登録の複雑な設定は、設定モーダルから既存の専用ダイアログへ遷移する。既存のステータスバーや検索画面、操作メニューの入口はショートカットとして残し、同じ設定状態を更新する。初期化の対象はアプリ設定に限定し、再開タブとアクティブタブからなるセッション状態は保持する。設定を一箇所へ集約しつつ既存の操作習慣と作業復元を壊さないため、この構成を採用する。
