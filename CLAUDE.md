# 開発方針

- 既存の優秀かつMITライセンスのライブラリ使用を優先する。
- 車輪の再発明は可能な限り行わない。

## テスト

- 1仕様につき1個以上のテストの構造にする。過去の仕様を失って手戻りが発生することを防ぐため。
- テストできない関数でもテスト関数を作り、中身は仕様を説明するコメントだけにする。

## 生成ファイル

- `ui/generated` はRust側のIPC定義から `npm run sync:ipc` で生成する。
- 通常のビルドはGitの作業ツリー状態に依存させない。`npm run build` では生成ファイルのGit差分を検査しない。
- CIやコミット前に `npm run check:generated` を実行し、生成結果の差分を確認する。
- IPC定義を変更した場合は、生成された `ui/generated` の差分もコミット対象に含める。

## リリース

- リリースは `npm run release -- <major.minor.patch>` を実行する。
- このコマンドは版番号の同期、検証、コミット、注釈付きタグ作成、GitHubへのpushまで行う。
- 通常の `git push` でリリース処理を行わない。
